# 이 파일을 복사해서 .env 로 저장하고 값을 채우세요:  cp .env.example .env
GEMINI_API_KEY=AIza...여기에_발급받은_키...

# (선택) 사용할 모델. 기본값 gemini-2.5-flash. 정확도 높이려면 gemini-3.5-flash
GEMINI_MODEL=gemini-2.5-flash

# (선택) 포트. 기본값 3000
PORT=3000
